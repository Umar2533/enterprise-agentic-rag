# API Reference

Base URL:

```text
http://localhost:8000/api/v1
```

Endpoints:

- `GET /health`
- `POST /upload/document`
- `POST /ingestion/upload` legacy alias
- `POST /chat`
- `GET /vectors/provider`
- `GET /collections`
